import { HideFormIfPathStartWithFormuleDirective } from './hide-form-if-path-start-with-formule.directive';

describe('HideFormIfPathStartWithFormuleDirective', () => {
  it('should create an instance', () => {
    const directive = new HideFormIfPathStartWithFormuleDirective();
    expect(directive).toBeTruthy();
  });
});
