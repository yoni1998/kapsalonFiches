import { BiggerInputDirective } from './bigger-input.directive';

describe('BiggerInputDirective', () => {
  it('should create an instance', () => {
    const directive = new BiggerInputDirective();
    expect(directive).toBeTruthy();
  });
});
