import { AddOrHideFormuleTitleDirective } from './add-or-hide-formule-title.directive';

describe('AddOrHideFormuleTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new AddOrHideFormuleTitleDirective();
    expect(directive).toBeTruthy();
  });
});
