import { FormuleFieldsetDirective } from './formule-fieldset.directive';

describe('FormuleFieldsetDirective', () => {
  it('should create an instance', () => {
    const directive = new FormuleFieldsetDirective();
    expect(directive).toBeTruthy();
  });
});
