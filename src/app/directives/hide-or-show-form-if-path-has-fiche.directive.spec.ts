import { HideOrShowFormIfPathHasFicheDirective } from './hide-or-show-form-if-path-has-fiche.directive';

describe('HideOrShowFormIfPathHasFicheDirective', () => {
  it('should create an instance', () => {
    const directive = new HideOrShowFormIfPathHasFicheDirective();
    expect(directive).toBeTruthy();
  });
});
