import { CopyBtnDisableDirective } from './copy-btn-disable.directive';

describe('CopyBtnDisableDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyBtnDisableDirective();
    expect(directive).toBeTruthy();
  });
});
