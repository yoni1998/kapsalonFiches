import { RemoveAddFicheBtnDirective } from './remove-add-fiche-btn.directive';

describe('RemoveAddFicheBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveAddFicheBtnDirective();
    expect(directive).toBeTruthy();
  });
});
