import { AddOrUpdateDirective } from './add-or-update.directive';

describe('AddOrUpdateDirective', () => {
  it('should create an instance', () => {
    const directive = new AddOrUpdateDirective();
    expect(directive).toBeTruthy();
  });
});
